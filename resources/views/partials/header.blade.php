<header class="bg-blue-900 text-white text-sm">
    <div class="max-w-7xl mx-auto flex flex-col md:flex-row justify-between items-center px-4 py-2">
        <div>
            <i class="fas fa-location-dot"></i> {{ setting('contact_address', trans('messages.contact.address')) }} |
            <i class="fas fa-phone"></i> {{ setting('contact_phone', '+225 07 07 07 07') }} |
            <i class="fas fa-envelope"></i> {{ setting('contact_email', 'contact@jatsmanor.ci') }}
        </div>
    </div>
</header>